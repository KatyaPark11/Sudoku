syntax = "proto3";

package sudoku;

service SudokuService {
  rpc Solve(SudokuRequest) returns (SudokuResponse);
}

message SudokuRequest {
  string puzzle = 1; // строка с судоку, например, 81 символов или в виде строки
}

message SudokuResponse {
  string solution = 1; // решённое судоку
}